
    import styled from "@emotion/styled";
import styled from "@emotion/styled";

    export const Modal = styled.div(props => ({
        
        ...props.styled
      }))
export const Modal = styled.div(props => ({
        
        ...props.styled
      }))
  