import styled from "@emotion/styled";

export const SlectorWrapper = styled.div((props: any) => ({}));

export const Input = styled.input((props: any) => ({
  width: "100%",
  padding: "3px",
  border: "none",
  "&:focus": {
    outline: "none"
  }
}));
